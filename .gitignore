*.nvda-addon
*.pyc
__pycache__/
.scons*
build/
dist/
*.pot
*.mo
.vscode/
.idea/
.DS_Store
Thumbs.db

# SCons-generated artifacts inside addon/ (regenerated by `scons`)
addon/manifest.ini
addon/doc/

# Captured page snapshots stay on the developer's machine. They are real
# browsing: URLs (query strings and all) plus previews of page text. Never
# commit them. tests/test_replay_corpus.py skips itself when this is absent,
# so a fresh clone still runs green.
tests/fixtures/local/
